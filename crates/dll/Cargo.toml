[package]
name = "dll"
version = "0.1.0"
edition = "2021"

[lib]
name = "alembic"
path = "src/lib.rs"
crate-type = ["cdylib"]

[dependencies]
tarpc = { workspace = true }
tokio = { workspace = true }
